{% import '@CustomAlerts/macros.twig' as alertsMacro %}
{% for alert in triggeredAlerts %}
{{ 'CustomAlerts_MailAlertContent'|translate(alert.name, alert.reportMetric|default(alert.metric), alert.reportName|default(alert.report), alertsMacro.metricChangeDescription(alert)) }}
>> {{ 'CustomAlerts_EditAlert'|translate }} {{ piwikUrl }}{{ linkTo({'module': 'CustomAlerts', 'action': 'editAlert', 'idAlert': alert.idalert, 'idSite': alert.idsite, 'period': alert.period, 'date': 'yesterday', 'token_auth': false, 'convertToUnicode': false, 'format': false, 'method': false, 'trigger': false})|raw }}

{% endfor %}