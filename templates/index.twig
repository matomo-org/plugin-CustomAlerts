{% extends 'admin.twig' %}

{% set title %}{{ 'CustomAlerts_ManageAlerts'|translate }}{% endset %}

{% block content %}
    <div
        vue-entry="CustomAlerts.ListAlertsPage"
        content-title="{{ title|json_encode|e('html_attr') }}"
        alerts="{{ alerts|default(null)|json_encode|e('html_attr') }}"
    ></div>
{% endblock %}